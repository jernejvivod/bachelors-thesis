function [r, g, b] = heatmap_cols(minimum, maximum, value)
    ratio = 2*(value-minimum)/(maximum - minimum);
    b = max(0, 255*(1 - ratio))
    r = max(0, 255*(ratio - 1))
    g = 255 - b - r
    return r, g, b
end